name: desktop

on:
  push:
    tags:
      - "desktop-v*"
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @zlog/web check
      - run: pnpm --filter @zlog/desktop test
      - run: pnpm --filter @zlog/desktop build
      - run: pnpm --filter @zlog/desktop build:standalone
      - run: xvfb-run -a pnpm --filter @zlog/desktop test:smoke

  build:
    needs: smoke
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @zlog/desktop package
      - uses: actions/upload-artifact@v4
        with:
          name: zlog-desktop-${{ matrix.os }}
          path: apps/desktop/release/*
          if-no-files-found: error

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/desktop-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: zlog-desktop-*
          merge-multiple: true
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          # 只挂安装器产物：asar:false 的全量包有数百个内部文件，
          # 逐个上传会触发 GitHub 次级限流（Task 12 实测 403/rate-limit）
          files: artifacts/**/Zlog*
          draft: true
